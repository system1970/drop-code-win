# Fetch prebuilt (N-API) binaries for native modules instead of compiling.
# node-pty ships an Electron-compatible prebuild, so no C/C++ toolchain is required.
runtime=electron
registry=https://registry.npmjs.org/
